For run with bash.sh
	-api-gateway -> no problem , just run bash run.sh
	-user microservice ->
		- delete the file in the migration with the name "XXXXXXX_comment.py" (only if u have it)
		-run this command in sequence:
			- export FLASK_ENV=development
			- flask db migrate -m "comment change"
			- flask db upgrade
			- delete cache browser (only if you have problem)
		-run command: bash run.sh